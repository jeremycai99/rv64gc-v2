obj_dir/
tests/**/obj/
*.o
*.elf
*.hex
*.vcd
*.stackdump

# Root-level ad hoc simulation outputs
/.claude/
obj_iv/
/*.out
/*.stdout
/*.stderr
/*.log
/*.jou
/*.wdb
/*.mxd
/*.pb
/tmp_result.txt
/*_objdump_*.txt
/build/

# DSim simulator artefacts
dsim_work/
dsim_linux_work/
dsim_core_synth_work/
dsim_image/
dsim.env
dsim*.log
dsim*.jou
metrics.db
*.mxd

# Verilator simulator artefacts
verilator_work/
verilator_linux_work/
verilator_linux_work*/

# cocotb + Python venv (cocotb verif flow not used — ignore the whole dir)
venv_cocotb/
verif/
**/__pycache__/
*.pyc

# Benchmark runner outputs and local licensed benchmark images
benchmark_results/
linux_boot_results/
tests/spec/external/
tests/spec/work/

# Baseline pipe.v1 traces (large, regenerable)
traces/

# Editor / OS / compiled-lib local cruft (never release docs or logs)
.DS_Store
*.swp
*.swo
*~
*.bak
*.orig
*.so
*.a

# Local agentic-dev guidance (kept locally for Claude Code/Codex/etc., not published)
/AGENTS.md

# Superpowers working space (specs/plans — local-only; published design docs live in doc/)
/docs/

# Archived xsim docs (xsim simulator dropped — kept locally under doc/archive/, not published)
doc/archive/xsim_workflow.md
doc/archive/xsim_lessons_learned.md

# All Verilator scratch builds (DSim is the signoff target; these are regenerable)
verilator_*/

# Session scratch logs
logs/

# Vendored CoreMark-PRO upstream source
tests/coremark-pro/external/

# log/ scratch (session run dirs); keep only the intentional RC verification reference set
log/*
!log/README.md
!log/compliance.log
!log/signoff.log
!log/boot.log
!log/stage5_perf_profile_trajectory.log
